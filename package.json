{
  "name": "image-transform-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for image transformation",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "image",
    "transform"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.26.0"
  }
}
